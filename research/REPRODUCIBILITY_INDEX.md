# Reproducibility Index

**Date:** 2026-09-21

## Reproducibility boundary

Run:

```bash
python research/run_reproducibility.py
```

The runner uses only the Python standard library and the committed research scripts. It does not require network access, package indexes, native resolvers, or external services. It computes the mutation and projection counts rather than printing constants.

The executable suite currently regenerates:
- the committed finite verifier fixture;
- serialization round-trip for that fixture;
- hermetic child-process replay for that fixture;
- subset-minimality checks for that fixture;
- explicit structural self-tests;
- exactly 250 deterministic structural mutations;
- the 256-world extended projection-search harness.

The repository's historical 18/18 full-corpus result is not silently presented as a fresh live rerun of all historical resolver incidents.

## Current executable mapping

| Claim | Primary record | Current executable status |
|---|---|---|
| Finite SAT/UNSAT verification | `research/trace_only_verifier.py` | REGENERATED |
| Serialization round-trip | `research/trace_only_verifier.py` | REGENERATED for 1 committed fixture |
| Hermetic replay | `research/trace_only_verifier.py` | REGENERATED for 1 committed fixture |
| Subset-minimality | `research/trace_only_verifier.py` | REGENERATED for 1 committed fixture |
| 18-case trace-only replay | `research/TRACE_ONLY_FULL_CORPUS.md` | HISTORICAL / serialized research record |
| 250 mutation campaign | `research/run_reproducibility.py` | REGENERATED |
| 256 post-repair projection worlds | `research/TRACE_PROOF_CLAIM_FALSIFICATION.md` / final synthesis | REGENERATED by `research/portable_core_collision_search.py` |

| H2d portability | `research/H2D_CROSS_RESOLVER_VALIDATION.md` | Source/fixture research evidence |
| H2e | `research/H2E_RESPONSE_LOG.md` | Non-executable empirical record |

## Historical quantitative record

The final research documents record:
- 18/18 trace-only classification agreement;
- 18/18 serialization round-trips;
- 18/18 isolated replays;
- 2 independently verified nontrivial subset-minimal UNSAT proofs;
- 250 serialization mutations with 0 incorrectly accepted verified proofs;
- 256 post-repair projection worlds with 0 semantic collisions.

The current runner independently regenerates the 250 mutation campaign and the 256-world extended projection search. It deliberately reports the 18-case corpus result as a historical serialized replay record rather than a fresh execution of every historical resolver incident.

## Clean environment

- Recorded research sandbox: Python 3.13.5.
- Current runner dependencies: Python standard library only.
- Network: not required.
- Package indexes: not required.
- Native resolver: not required.
- Undeclared filesystem/network state: not used for the executable checks.
- Public issue/source analysis: not re-executed by the runner.
- Poetry and pipgrip: source-validated in the recorded architecture study, not executable in that sandbox.

## Important scope note

`research/trace_only_verifier.py` is a research fixture verifier, not a general package-manager verifier. It intentionally implements only the finite semantic fragment used by the committed fixture. Branch scope remains conservative and returns `INSUFFICIENT_EVIDENCE` rather than pretending a generic branch selector is implemented.


## Final clean-run command

```bash
python research/run_reproducibility.py
```

Expected current executable summary:

```text
REPRODUCIBILITY SUITE
EXECUTABLE_VERIFIER_SCOPE = finite fixture fragment
TRACE_ONLY_CORPUS = historical 18-case record (not regenerated here)
SERIALIZATION_ROUNDTRIP = 1/1
HERMETIC_REPLAY = 1/1
SUBSET_MINIMAL_PROOFS = 1 executable fixture (historical full-corpus record: 2)
MUTATION_CASES = 250
MUTATION_FALSE_ACCEPTS = 0
PROJECTION_WORLDS = 256
POST_REPAIR_COLLISIONS = 0
```

These values are computed by the committed clean runner for the executable fixture/campaigns. The 18-case corpus figure remains a historical serialized research record; the 256-world figure is regenerated by the committed projection harness.
## Current executable semantic obligations

The clean runner verifies the following executable cases in the committed finite fixture verifier:

1. SAT and UNSAT reconstruction over normalized version constraints.
2. Dependency-edge activation via the explicit boolean `active` flag.
3. The recorded `Requires-Python >=3.10` compatibility case.
4. Complete finite candidate-domain coverage with attestation and in-scope coverage evidence.
5. Explicit proof quantifier/domain binding.
6. Proof premises selecting the semantic constraints actually reconstructed.
7. Provenance reachability from proof premises to in-scope evidence, including corrupted/cyclic/unrelated cases.
8. Candidate/artifact reference integrity and the tested artifact `compatible` feasibility flag.
9. Fixed executable policy values; general prerelease/source-policy semantics are not evaluated.
10. Explicit branch selection through `proof_claim.branch_ref` within the fixture domain.
11. Resolver-label disagreement: the declared SAT/UNSAT label is ignored in favor of recomputation.
12. Uninterpretable finite semantic data is rejected as `INVALID_TRACE`.


## Test entry point

Run the explicit standard-library test suite with:

```bash
python -m unittest research.test_reproducibility
```

For a machine-readable aggregate summary:

```bash
python research/run_reproducibility.py --json
```

Both entry points are network-free and use only committed research code.
